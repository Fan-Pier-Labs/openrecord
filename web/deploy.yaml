platform: "fargate"
app_name: "mychart"
dockerfile: "Dockerfile"
aws:
  profile: "fanpierlabs"
  region: "us-east-2"
public:
  domain: "openrecord.fanpierlabs.com"

task:
  cpu: 1024
  memory: 2048
  spot: true
  replicas: 1
  ephemeral_storage: 21
  port: 8080

secrets:
  - arn:aws:secretsmanager:us-east-2:555985150976:secret:SENTRY_AUTH_TOKEN-HqokeI


allow_create: true
